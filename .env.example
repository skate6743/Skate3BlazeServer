# Your server's public IP address.
# Leave blank to auto-detect via api.ipify.org (recommended for most setups).
# Set this if auto-detect returns the wrong IP, or you're hosting on a LAN/VPN.
SERVER_IP=
